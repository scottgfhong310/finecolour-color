# finecolour-color — 設計決議

> 版本 v1.0｜最後更新 2026-08-01

> 「怎麼用」在 [README](README.zh-Hant.md)；本檔記**為什麼長這樣**。
> 家族共同規範不重複，只標引用：
> [DESIGN_GUIDELINES](https://github.com/scottgfhong310/nodeapp-webapp-family/blob/main/DESIGN_GUIDELINES.md)
> §11.1（色票明細卡）、§5.13（兩類 chips）、§6.2（資料的多語名稱）。

---

## 1. 這支 app 在回答什麼

與家族另外三支色彩 registry 同一個問題——**「這個色號是什麼？我怎麼把它貼進 CSS？」**——
所以明細卡照抄 §11.1 的骨架。差異只在「這個品牌有哪些事實值得放進來」。

Finecolour 有兩件事實是另外三個品牌沒有的，它們決定了這支 app 的形狀。

### 1.1 一個品牌，三個色號空間

| | marker | fineliner | acrylic |
| --- | --- | --- | --- |
| 色數 | 480 | 48 | 120 |
| 色碼 | 色系字首＋**全域唯一號碼**（`BV321`／`R140`／`CG271`） | 兩位數（`02`／`03`） | 三位數（`500`–`619`，連續無缺） |
| 官方色名 | **英文** | **中文** | **無——原廠不發佈** |
| 色系分群 | 23 群 | 無 | 無 |
| 媒材 | 酒精 | 水性針管尖 | **水性壓克力（不透明）** |
| 產品線 | EF100／EF101／EF102／EF103 | EF300 | EF600 |

三者的色碼**零交集**，所以「號碼在品牌內全域唯一」這句話對整個品牌都成立。

號碼在品牌內全域唯一，所以官方的套裝表可以只印裸號碼——這也是**色碼不可再分解**的原因
（不像 COPIC 的 `BV02` 能拆成「色系＋群＋明度」兩個軸）。因此這支 app **沒有矩陣**，
排序就是「色系 → 號碼」。

彩針筆與壓克力筆都是**另一個編號空間**，不是麥克筆的某個色系，所以它們在分組 chips 裡
各自成一群、排在 23 個色系之後並以分隔線隔開。把它們混進色系列表會讓人以為
`02` 和 `BV02` 是同一套編號。

**壓克力筆（EF600）有兩點與另外兩個空間都不同**：
① **原廠不發佈色名**——`official: 'none'`，主識別是色號，畫面上**明講「原廠不發佈色名」而不留白**
（留白會被讀成「我們沒抽到」，那是錯的答案；同 `enmy-color` 的處置）。
② **它是不透明媒材**，所以 `nearestFinecolour` 預設仍只比 marker——
把壓克力混進酒精麥克筆的推薦會誤導，兩者的疊色行為完全不同。

⚠️ EF600 屬**法卡勒（上海）**，與前四代麥克筆的肇慶製造廠是不同法人；
製造商官網沒有它的色譜，本線資料來自經銷商商品頁的官方套組圖（故 hex 標 approximate）。

### 1.2 官方色名不一定是英文〔本 app 最容易做錯的一條〕

DESIGN_GUIDELINES §6.2 說「**官方英文名恆為主名**」——那條規則的前提是
「品牌會發佈英文色名」。Finecolour 的彩針筆**打破了這個前提**：官方只有中文名。

所以資料多帶一欄 **`official`**（`'en'` 或 `'zh'`），呼叫端一律用
`FinecolourColorLib.officialName(color)` 取主名、`localName(color, lang)` 取輔助行，
**不可直接讀 `.name`**。直接讀的後果是：彩針筆會顯示成「Lemon Yellow」，
而那是我們這邊的譯名，**拿去問賣家對不到任何東西**——app 就從「可驗證的參考」
變成「看起來很像的東西」。

§6.2 的**精神**（官方名恆為主名，因為它是可驗證的識別憑據）完全成立；
會壞掉的是把「官方名」寫死成「英文名」的那個實作假設。

### 1.3 產品線可得性是這支 app 的招牌

同一支色不見得每一代都有出。這是 Finecolour 唯一能回答
「**我手上是 1 代，這支買得到嗎**」的資訊，所以做成**篩選 chips**（多選、帶勾號）
放在分組 chips 之下，明細卡也用徽章列出五條線的有無。

資料來源是 EF102 色譜上實印的符號（○＝EF100、●＝EF101、■＝EF103），
並以各代自己的宣傳單獨立驗證過——三條線的色碼集合逐一相同
（EF100 265/265、EF101 188/188、EF103 252/252）。

---

## 2. 兩類 chips 不可混用（§5.13）

| | 分組 chips（色系） | 篩選 chips（產品線） |
| --- | --- | --- |
| 作用 | 換一種方式排列同一批 | 縮小看得到的範圍 |
| 選取 | 單選互斥、**恆有一個 active** | 多選 toggle、可全部取消 |
| 記號 | 實心底、無勾號 | 實心底 **＋ 勾號** |

三個對應的處置：

1. **「全部 648」自己是 bar 裡的第一顆 chip**——分組 chips 取消不掉，
   「看全部」的出口若只放在側鍵，使用者不會去找。
2. **搜尋時整條分組 bar 收起**——搜尋結果跨色系，留著一顆亮起的 chip 會讓人
   以為看到的是那一群的色。「全部」不受此限（它自己是 chip）。
3. **產品線 chips 只在「這個分組下該線有色」時才顯示**，並把「已選但目前不存在」
   的值剔除；只剩一種取值時整列收起（無從篩起）。

---

## 3. 資料從哪裡來

**不手改。** `data/*.js` 是 `db_artcolor`（家族色彩資料庫，System of Record）的匯出產物，
由 `My Projects/Art Colour/export/a3-export.js` 產生，`--check` 逐位元組驗證。

上游是**製造商官網**（`finecolour.com.cn`）的官方宣傳單 JPEG。圖上自述
「本色譜顏色僅供參考，馬克筆準確顏色請以實際畫線為準」——**hex 是螢幕近似值，
不是官方規格**，明細卡與產出的 CSS 檔頭都照實寫。

抽取的驗收全部以**紙上自己印的數字**為準：號碼 0–479 連續無缺、每個套組筆數等於
標題宣稱、同系列巢狀且每級恰好 +12。這些不是我們定的條件，所以過了就是真的過了。

### 3.1 一個值得記的坑

色譜的 legend 寫「EF100 共240色／EF101 共160色／EF103 共240色」，
但紙上實印的符號是 **265／188／252**。當時**沒有選邊**，兩個數字雙軌保留；
後來抽了三條線各自的宣傳單才知道 legend 是「**膚色擴充前的基準色數**」
（EF100「+新增25个肤色」、EF101「+新增28个肤色」、EF103 標題即「240 + 12 肤色」）。

**若當初把符號「修正」成 legend 的數字，會弄丟 65 支真實存在的筆。**
來源內部數字對不上時，先假設「兩個數字在回答不同問題」，再假設有一方是錯的。

---

## 4. `nearestFinecolour` 預設只比 marker

比對器預設 `space: 'marker'`——彩針筆是**另一種筆**（水性、針管尖），
拿它回答「該用哪支麥克筆」是答非所問。要比彩針筆得明確傳 `space: 'fineliner'`。

同樣的道理，`opts.line` 可再限定產品線（`'EF101'` 只比 2 代有出的 188 色）——
**手上沒有的筆別推薦**。慣例同 `faber-castell-color` 的 `nearestFC` 預設只比 `ag`。

---

*[MIT](LICENSE) © 2026 [Scott G.F. Hong](https://github.com/scottgfhong310)*
